<section class="container" data-section-id="{{ section.id }}">
  <div class="page-head">
    <p class="eyebrow">{{ 'general.search.title' | t }}</p>
    <h1 class="display h1">
      {%- if search.performed -%}
        {{ 'general.search.results_for' | t: terms: search.terms }}
      {%- else -%}
        {{ 'general.search.heading' | t }}
      {%- endif -%}
    </h1>
    <form action="{{ routes.search_url }}" method="get" role="search" class="search-form inline-form" style="margin-block-start:22px">
      <label class="visually-hidden" for="SearchInput">{{ 'general.search.placeholder' | t }}</label>
      <input id="SearchInput" type="search" name="q" value="{{ search.terms | escape }}" placeholder="{{ 'general.search.placeholder' | t }}" autocomplete="off">
      <input type="hidden" name="options[prefix]" value="last">
      <button class="btn" type="submit">{{ 'general.search.submit' | t }}</button>
    </form>
  </div>

  {%- if search.performed -%}
    {%- paginate search.results by 24 -%}
      {%- if search.results_count == 0 -%}
        <div class="empty">
          <h2 class="display h2">{{ 'general.search.no_results' | t: terms: search.terms }}</h2>
          <a class="btn btn--outline" href="{{ routes.collections_url }}">{{ 'collections.general.browse' | t }}</a>
        </div>
      {%- else -%}
        <p class="label" style="margin-block-end:20px">{{ 'general.search.results_count' | t: count: search.results_count }}</p>
        <div class="grid grid--products">
          {%- for item in search.results -%}
            {%- case item.object_type -%}
              {%- when 'product' -%}
                {% render 'product-card', product: item, lazy: true %}
              {%- when 'article' -%}
                <article class="card reveal"><div class="card__body">
                  <span class="label">{{ 'blogs.article.label' | t }} &middot; <span class="code">{{ item.published_at | date: format: 'abbreviated_date' }}</span></span>
                  <h3 class="card__title"><a href="{{ item.url }}">{{ item.title }}</a></h3>
                  <p class="card__meta">{{ item.excerpt_or_content | strip_html | truncatewords: 24 }}</p>
                </div></article>
              {%- when 'page' -%}
                <article class="card reveal"><div class="card__body">
                  <span class="label">{{ 'general.page.eyebrow' | t }}</span>
                  <h3 class="card__title"><a href="{{ item.url }}">{{ item.title }}</a></h3>
                  <p class="card__meta">{{ item.content | strip_html | truncatewords: 24 }}</p>
                </div></article>
            {%- endcase -%}
          {%- endfor -%}
        </div>
        {% render 'pagination', paginate: paginate %}
      {%- endif -%}
    {%- endpaginate -%}
  {%- endif -%}
</section>

{% schema %}
{
  "name": "t:sections.main_search.name",
  "settings": []
}
{% endschema %}
